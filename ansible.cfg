[defaults]
# Look for roles relative to the repo root, not the playbook directory.
roles_path = roles
inventory  = inventory/hosts
# Suppress deprecation warning for the old top-level fact injection behaviour.
inject_facts_as_vars = False
