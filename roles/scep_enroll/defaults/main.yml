---
# scep_ca_name, scep_challenge_password, cert_org, and cert_country are
# provided via group_vars/all/vault.yml — set there, not here.

# Where the issued key and certificate are stored on the client.
cert_key_path: "/etc/pki/tls/private/scep-wifi.key"
cert_path: "/etc/pki/tls/certs/scep-wifi.crt"

# Certificate subject. CN combines hostname + last 6 chars of machine-id +
# a group-derived tag (resolved at runtime in tasks/main.yml).
# cert_subject_cn is overridden once the tag is resolved.
cert_subject_cn: "{{ ansible_facts['hostname'] }}-{{ ansible_facts['machine_id'][-6:] }}"
cert_subject: "CN={{ cert_subject_cn }},OU=Linux,O={{ cert_org }},C={{ cert_country }}"
